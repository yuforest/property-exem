<%= form_with model: @property do |f| %>
  <% if @property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property.errors.count, "error") %> prohibited this property from being saved:</h2>

      <ul>
      <% @property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :rent %>
    <%= f.number_field :rent %>円
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :age %>
    <%= f.number_field :age %>年
  </div>

  <div class="field">
    <%= f.label :remark %>
    <%= f.text_area :remark %>
  </div>
  
  
    <%= f.fields_for :nearest_stations do |s| %>
      <%= render "nearest_station_fields", f: s %>
    <% end %>
    
    <%= f.fields_for :nearest_stations do |s| %>
      <%= render "nearest_station_fields", f: s %>
    <% end %>
    <%= link_to_add_fields "住所追加", f, :nearest_stations %>
    
    
    
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
